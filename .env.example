# Optional: keep the database outside the repository in production.
TIME_TYE_DB_PATH=time-tye/data/time_tye.db

# Set this in Streamlit secrets or the deployment environment.
# SESSION_SECRET=replace-me